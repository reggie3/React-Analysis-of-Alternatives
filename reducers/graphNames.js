export default function graphNames(graphNames=["Criteria Weights",
        "Raw Scores",
        "Normalized Scores",
        "Weighted Scores"], action) {
    switch (action.type) {
        default:
            return graphNames;
    }
}